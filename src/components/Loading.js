function Loading() {
  return (
    <div className="w-full h-full flex justify-center items-center">
      <img src="loading.png" alt="loading.png" width={32} height={32} className="animate-spin" />
    </div>
  );
}

export default Loading;
